#include "c_game.hpp"

int main() {
  /*for (const auto& dir: diff::queen) for (const auto&[a, b]: dir) {
    std::cout << (int)a << ' ' << (int)b << '\n';
  }*/
  std::cout << "\nHello World!\n";
}//